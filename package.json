{
  "name": "fiber",
  "version": "1.0.0",
  "description": "fiber landing page challenge from codewell",
  "scripts": {
    "dev": "parcel ./src/index.html",
    "prebuild": "npx rimraf build",
    "build": "parcel build --public-url ./ ./src/*.html --dist-dir build"
  },
  "author": "John Martin De Guia",
  "license": "ISC",
  "dependencies": {
    "@popperjs/core": "^2.9.2",
    "bootstrap": "^5.0.2",
    "parcel": "^2.0.0-beta.2"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0-beta.2",
    "sass": "^1.35.1"
  }
}
